
/**
 * Basic shared schema placeholder for FreeMind Vision
 * ---------------------------------------------------
 * This file exists to prevent build errors in the backend.
 * You can add your real Zod/Drizzle/Prisma schemas later.
 */

export const schema = {
  appName: "FreeMind Vision",
  version: "1.0.0",
  description: "Shared schema placeholder used for backend builds.",
};

// Optional: export types
export type SchemaType = typeof schema;
